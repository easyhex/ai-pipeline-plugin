# ai-pipeline

Плагин для Claude Code — пайплайн AI-разработки из 12 команд с обкаткой пользователя frontier-раундами и явным гейтом подписи спеки, автоматическим критиком (senior engineer) на двух стадиях, системой накопления уроков, памятью Serena для стабильных знаний о проекте и гейтом визуальной верификации через Playwright MCP — всего 6 слоёв контекста.

## Что вы получаете

После установки плагина вы можете создать любой новый проект одной командой и вести каждую фичу от начала до конца через крошечный набор слэш-команд:

| Команда | Когда |
|---|---|
| `/init "<описание>"` | Один раз при старте проекта |
| `/plan-improve "<изменение>"` | Уточнить мастер-план (без кода) |
| `/feature "<описание>"` | Новая функциональность — полный автоматический пайплайн |
| `/improve "<изменение>"` | Изменение существующего поведения — полный пайплайн |
| `/fix "<баг>"` | Отладка + автозапись урока |
| `/lesson` | Записать урок вручную (редко) |
| `/remember "<факт>"` | Записать факт о проекте в память Serena (редко) |
| `/questionnaire "<тема>"` | Сгенерировать опросник требований для клиента/эксперта (знание в чужой голове) |
| `/release` | Срезать релиз: semver, человеческий CHANGELOG, дифф требований, локальный тег |
| `/resume` | Продолжить прерванный прогон — позиция выводится из артефактов, не из памяти |
| `/validate` | Проверить shipped-фичи против их критериев успеха реальными свидетельствами |
| `/deprecate "<фича>"` | Намеренно вывести/сломать фичу — impact-скан + закоммиченный план миграции |

Вы никогда не вводите `/brainstorm`, `/plan`, `/build`, `/critic`, `/verify` или `/finish` — это внутренние фазы двенадцати команд выше.

## Установка

```bash
claude plugin marketplace add easyhex/ai-pipeline-plugin
claude plugin install ai-pipeline@ai-pipeline-marketplace
```

Всё. 12 команд и агент `senior-critic` теперь доступны глобально в любом проекте.

## Создание нового проекта

```bash
mkdir my-new-app
cd my-new-app
claude
> /init "todo-приложение с realtime синхронизацией"
```

`/init` работает и в пустой папке, И на существующей кодовой базе (adopt-режим: Master Plan выводится из реального кода, подтверждается построчно, ничего не перезаписывается). Он выполнит:
1. Автоустановку отсутствующих плагинов-зависимостей (с подтверждением `[y/n]`)
2. Короткое интервью frontier-раундами (ставки / пользователь / стек / сценарии / ранжирование качества / принудительный НТТ-раунд)
3. Запись файлов проекта (CLAUDE.md, docs/, docs-meta/, .claude/)
4. Заполнение 3 файлов мастер-плана и построчное подтверждение каждой строки с вами ДО первого коммита — неподтверждённое остаётся с меткой `(proposed — unconfirmed)`
5. `git init` и `bd init` (если `bd` установлен)
6. Первый коммит

После `/init` вы начинаете писать фичи:

```
> /feature "добавить регистрацию пользователей через magic link"
```

Пайплайн сначала интервьюирует вас (frontier-раунды нумерованных вопросов с рекомендованными ответами — см. `docs-meta/ELICITATION.md`), проигрывает спеку на явную подпись — и только затем едет автономно: план, beads-задачи, TDD-цикл с автокоммитами, критик gate-2, верификация, merge или PR. Останавливается только когда критик находит что-то, требующее вашего решения, или на сбоях.

## Возможности пайплайна

- **Петля валидации (v1.0.0)** — каждая спека объявляет фальсифицируемые критерии успеха; `/validate` спрашивает у реальности свидетельства и маршрутизирует промахи в репритеризацию roadmap; `/deprecate` делает намеренный слом выразимым (план миграции, протокол «слом задуман» для критика, BREAKING-строки → мажорный бамп); `/lesson distill` компилирует кучу уроков в маленький файл правил, который ground читает первым; находки критика проходят fresh-context верификацию (до вас доходят только воспроизведённые); сознательные отказы живут в базе out-of-scope, которую проверяет критик.
- **Слой механизма (v0.7.0)** — принуждение в харнесе, а не в прозе: хуки плагина (PostToolUse-журнал как ground truth, Stop-гейт не даёт закрыть сессию посреди верификации, PreToolUse-блокировщик отказывает git merge/push при провальном вердикте гейта или незакрытых Critical, PreCompact-снапшоты, инъекция контекста в промпт), машинные вердикты гейтов (JSON критика + verdict.json), run-state + `/resume`, весь bash гейтов вынесен в тестируемые `scripts/pipeline/`, скелет поведенческих promptfoo-тестов и запин Playwright MCP.
- **Слой математики (v0.6.0)** — классы проектов (вычислительные пропускают визуальный гейт и получают `docs/model.md`), таксономия оракулов для численного TDD (`docs-meta/NUMERICS_TESTING.md`), квантитативный гейт Phase 9c (доказывающие команды НТТ по сидам, pass^k, анти-переоценочные вердикты, run-manifest, advisory-мутационное тестирование), `@relation`-маркеры в коде с hash-отслеживанием suspect-связей, enum методов верификации (Test/Analysis/Inspection/Review) и критик, знающий численные режимы отказа (карантин утверждений, линза верификационных разрывов, сабботаж-чек).
- **Слой требований (v0.5.0)** — живые файлы ФТТ/НТТ с EARS-критериями приёмки и неизменяемой mid-идентичностью (`docs/requirements/`), таблица трассируемости (требование → спека → гейты → merge SHA), риск-реестр, питаемый override-ами гейтов, анализ аналогов, глоссарий, коммитаемые ADR и `/release` с человеческим CHANGELOG и секцией «требования изменились».
- **Слой обкатки (v0.4.0)** — единая каноническая техника интервью (`docs-meta/ELICITATION.md`: frontier-раунды, факты-vs-решения, линт гигиены вопросов, принудительный НТТ-раунд, типизированный долг `[NEEDS CLARIFICATION]`/`TBC`), гейт проигрывания спеки с явной подписью до кода, секции провенанса (решения пользователя отдельно от допущений машины) и `/questionnaire` для требований из чужих голов.
- **Гейт визуальной верификации (v0.3.0)** — для фронтенд-проектов фаза 9 запускает Playwright MCP, открывает URL'ы перечисленные в спеке (`## URLs to verify`), снимает скриншоты + accessibility-снимки и стопит пайплайн при console-ошибках или пустом рендере. Настройки в `.claude/settings.json` → `pipeline.visual_verify`.

## Зависимости

Плагин зависит от 4 других плагинов Claude Code. `/init` автоматически установит отсутствующие (с согласия):

- **superpowers** — TDD, brainstorming, отладка, верификация, code review
- **beads** — постоянное отслеживание задач между сессиями
- **context7-plugin** — актуальная документация библиотек через MCP
- **template-bridge** — 413+ специализированных агентов

Плюс одни CLI-инструменты, которые надо ставить вручную:

- **`bd`** (Beads CLI): `brew install beads` (macOS) или `curl -fsSL https://raw.githubusercontent.com/steveyegge/beads/main/scripts/install.sh | bash`
- **`uv`** (менеджер Python-инструментов): `brew install uv` (macOS) или `curl -LsSf https://astral.sh/uv/install.sh | sh`
- **`serena-agent`** (Python-инструмент, автоматически установится `/init` через `uv` если `uv` присутствует)

После `/init`, Serena MCP-сервер регистрируется глобально:

```bash
claude mcp add --scope user serena -- serena start-mcp-server --context claude-code --project-from-cwd
```

## Архитектура

Пайплайн состоит из четырёх автозагружаемых компонентов:

1. **12 пользовательских команд** (в `commands/`) — загружаются Claude Code автоматически
2. **Агент `senior-critic`** (в `agents/`) — загружается автоматически, вызывается на двух стадиях каждой фичи
3. **Хуки принуждения** (в `hooks/` + `scripts/hooks/`) — гейты уровня харнеса, no-op вне активных прогонов
4. **Шаблоны проекта** (в `assets/templates/`) — записываются в проект командой `/init`

Файлы проекта, которые пишет `/init`:

- `CLAUDE.md` — правила пайплайна и набор из 12 команд
- `docs/architecture.md` — мастер-план: целевая архитектура (≤300 строк)
- `docs/features.md` — мастер-план: список фич (≤500 строк)
- `docs/roadmap.md` — мастер-план: упорядоченные приоритеты (≤200 строк)
- `docs-meta/PIPELINE.md` — справочник по пайплайну
- `docs-meta/LESSON_FORMAT.md` — схема урока (4 YAML-поля + 3 предложения тела)
- `docs-meta/ELICITATION.md` — техника интервью (frontier-раунды, гигиена вопросов, маркеры)
- `docs-meta/SPEC_FORMAT.md`, `docs-meta/REQUIREMENTS_FORMAT.md`, `docs-meta/ADR_FORMAT.md` — схемы артефактов (EARS-грамматика, живые файлы требований, решения)
- `docs/risks.md`, `docs/glossary.md`, `docs/analysis/analogs.md` — риск-реестр, единый язык, анализ аналогов
- `docs/requirements/` — опросники для внешних стейкхолдеров (пишет `/questionnaire`)
- `.claude/settings.json` — хуки + включённые плагины (включая `ai-pipeline`)
- `.gitignore` — стандартные исключения
- `.claude/lessons/` — наполняется со временем командами `/fix` и `/lesson`
- `.serena/memories/` — слой памяти Serena (стабильные знания о проекте: конвенции + решения)

Полное описание дизайна — см. `docs/DESIGN_NOTES.md` и `docs/WORKFLOW_GUIDE_RU.md`.

## Обновление плагина

```bash
claude plugin install ai-pipeline@ai-pipeline-marketplace
```

Это перезатягивает плагин из источника. Существующие проекты продолжают работать — у них есть своя копия шаблонов с момента `/init`.

## Жёсткие правила (per-project CLAUDE.md)

После `/init` файл `CLAUDE.md` в проекте требует:

1. Никакого production-кода без падающего теста сначала
2. Никаких "готово" без запуска проверочной команды
3. Уроки применяются автоматически перед каждой фазой
4. Context7-запрос перед использованием любой библиотеки/фреймворка
5. Критик на gate-1 (после спеки) и gate-2 (после диффа)
6. Каждый зелёный TDD-цикл заканчивается `git commit`; каждый `/fix` пишет урок
7. Перед запуском `/feature` или `/improve`: прочитать `.serena/memories/` и загрузить любую память, соответствующую теме работы или затронутым файлам
8. Обкатка по `docs-meta/ELICITATION.md`: факты ищутся, решения спрашиваются frontier-раундами; явная подпись спеки на playback-гейте до любого плана и кода
9. Требования живут в `docs/requirements/` (EARS-критерии, неизменяемый `mid`); override-ы гейтов пишутся в `docs/risks.md`; шиппинг дописывает `docs/TRACEABILITY.md`

## Контрибуция

См. `CLAUDE.md` — правила для контрибьюторов плагина. Шаблоны в `assets/templates/` — это user-facing файлы; держите их в соответствии с тем `CLAUDE.md`, который они доставляют пользователям.

## Лицензия

MIT — см. [LICENSE](LICENSE).
