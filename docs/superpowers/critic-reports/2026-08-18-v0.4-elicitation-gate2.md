# Critic Review — Gate 2 — 2026-08-18

**Subject:** `git diff main..feature/v0.4-elicitation` (14 files, +401/−74) against spec `docs/superpowers/specs/2026-08-18-v0.4-elicitation-design.md` (APPROVED, D1–D4 user-confirmed)
**Reviewed against:** docs/architecture.md (n/a — plugin repo), CLAUDE.md (contributor rules), .claude/lessons/ (directory does not exist in this repo — 0 files)

Spec fidelity at a glance: items 1, 2, 3, 5, 6, 8 shipped; item 4 partial (sections mandated, gate-1 critic check missing); item 7 partial (weight exists, but /init's "default weight for future runs" goes nowhere and "critic pass count" effect absent). D1 shipped with wording drift, D2/D3/D4 shipped. Ledger: A1 in tension with shipped gate-2 behavior (inherited from the spec's own item 8), A2 NOT honored, A3 honored, A4 honored.

## Critical (must address before proceeding)

- The shipped source-of-truth template contradicts itself on the release's core behavior. `assets/templates/PIPELINE.md:14` still says of `/feature`: "The user fires this and walks away — only intervenes when the brainstorm has a real clarifying question or the critic surfaces a Critical finding," and `:8` still says `/init` "Asks ~3 clarifying questions" — while the same file's phase table (`:40`) mandates the blocking Phase 3.5 playback gate and the READMEs describe the new interview. Per-project `CLAUDE.md` declares PIPELINE.md the source of truth; an agent in a user project can cite `:14` to skip the interview and the sign-off, negating v0.4 entirely. Contributor hard rule 1 violation. — evidence: assets/templates/PIPELINE.md:8,14 vs :40; assets/templates/CLAUDE.md:4

## Important (strongly suggest addressing)

- `/fix` and the playback gate: undefined and misdocumented. Contributor CLAUDE.md rule 8 claims "`/improve` and `/fix` inherit it," and PIPELINE.md's phase table header says the phases (including 3.5) "run automatically inside `/feature`, `/improve`, `/fix`" — but `commands/fix.md` defines its own Phases 2–4 inline (Phase 3 critic → Phase 4 plan, no playback, no `**Approved by user:**` pre-condition). Whether a `/fix` run plays back its diagnosis is defined nowhere. Either give /fix a (light) playback or scope the claims. — evidence: CLAUDE.md:24 (rule 8); assets/templates/PIPELINE.md:33-41,51; commands/fix.md:48-66
- The run-state deviation is sound but recorded nowhere. Spec §3 says approval is "Recorded in run-state as `approvals.spec: true`" and §7 says weight is "recorded in run-state"; the implementation writes `**Approved by user:** YYYY-MM-DD (weight: …)` into the spec file and `weight:` into spec frontmatter. Judgment: the deviation is correct — no run-state exists until v0.7, and the Phase 4 pre-condition gives the spec-file line enforcement teeth a passive ledger field would lack. But it is stated in neither the spec, the CLAUDE.md v0.4.0 changelog line, nor ELICITATION.md; v0.7 hook work reading the approved spec will implement against `approvals.spec` and miss the actual mechanism. — evidence: spec §3, §7 vs commands/feature.md:155,164 and assets/templates/ELICITATION.md:88; `grep -rn run-state commands/ assets/` → empty
- A2 not honored: the spec's assumption ledger (part of the approved document) promises "Coverage-tree templates ship in the plugin (generic + numerics variant); chosen by one interview question." No coverage-tree template files exist in `assets/templates/` and no interview question selects a variant — only an inline example block in ELICITATION.md. — evidence: `ls assets/templates/` (9 files, no coverage templates); spec A2
- Announcement over-promises post-sign-off autonomy. feature.md:23 tells the user "After sign-off I'll only stop for Critical critic findings or failures," but Phase 8 Important-only explicitly stops and asks `continue / address` (feature.md:267). Same over-promise in README.md ("stopping only for Critical critic findings or failures") and README_RU.md. This is the spec's own A1-vs-item-8 tension shipped unresolved — the announcement asserts one side, the gate implements the other. — evidence: commands/feature.md:23 vs :267; README.md §Bootstrap; spec A1 vs §8
- `(proposed — unconfirmed)` tags are never cleared. init.md:272 promises "`/plan-improve` clears the tag when the item is later confirmed or reworked," but `commands/plan-improve.md` contains no mention of the tag — the clearing behavior lives only in a file /plan-improve never loads at runtime. Tags will accumulate in user projects' Master Plans indefinitely. — evidence: commands/init.md:272; `grep proposed commands/plan-improve.md` → only unrelated hits
- /init's stakes answer is a dead data path. Round 1 says stakes "Sets the default ceremony weight for future runs (`light` / `standard` / `deep`)," but no storage location is defined (not settings.json, not architecture.md) and `/feature` never reads a default — it re-asks the weight question every run. The claim misleads the user into thinking the answer persists. — evidence: commands/init.md Phase 2 Round 1 vs commands/feature.md:71; assets/templates/settings.json unchanged
- Spec item 4's gate-1 critic check is not implemented. "Gate-1 critic checks: material requirement outside User decisions must appear in Assumptions" — `agents/senior-critic.md` is untouched and feature.md's Phase 3 critic invocation (lines 115-124) does not instruct the check. The provenance sections exist but nothing audits them. — evidence: spec §4; `git diff --name-only` (no agents/ change); commands/feature.md:113-124
- Post-sign-off open decisions have no defined route. ELICITATION.md's "Decisions are never made silently" and feature.md's error-table row "Interview decision open → put it to the user as a frontier round, wait" are unscoped to Phase 2 — a decision surfacing mid-TDD (Phase 7) can be read as either "stop and ask" (breaking A1's autonomy promise) or "decide silently" (breaking ELICITATION.md). Define the post-approval route (e.g. assumption + TBC marker, surfaced at gate-2). — evidence: assets/templates/ELICITATION.md:5; commands/feature.md:565; spec A1
- The playback gate's only enforcement mechanism is untested, and T12/T15/T16 are token-presence tests. T12 greps three strings in feature.md — `Approved by user` is satisfied by either the Phase 3.5 record line or the Phase 4 pre-condition, so deleting the pre-condition (the gate's actual teeth) keeps the suite green; T15 passes if "Important-only" appears with any (even inverted) behavior attached; T16's `BEFORE the first commit` check does not verify Phase 4.5 actually precedes the commit phase; T11 catches only the literal phrase "proceed silently", not a paraphrase. Untested v0.4 behaviors: Phase 4 pre-condition, gate-1 Important→3.5 carry, forced NFR round, /questionnaire's two-question limit. — evidence: scripts/check.sh:125-172

## Nice to have

- feature.md:6 header still reads "(full auto pipeline)" — stale next to the rewritten frontmatter description.
- README_RU.md:42,58 "НТТ-раунд" — apparent typo for "НФТ" (нефункциональные требования); appears twice, so it propagates.
- D1 opt-out wording drift: the user-confirmed decision says «When grilling, ask one question at a time»; ELICITATION.md:21 ships `When interviewing, ask one question at a time.` A user following the spec's wording adds a line the doc never names.
- ELICITATION.md hygiene rule 3 ("not leading — must not embed the expected answer") vs rule 9 ("every question ships a ➡️ recommendation") — the reconciliation (neutral body; recommendation only in the ➡️ line) is implied, never stated; rule 12's "ask open-form" also collides with rule 9's "every question." A user editing this doc per-project will trip on it.
- Spec item 1 describes an "Aspect → Dimension → Slot tree"; ELICITATION.md's state-file example shows Aspect → Slot with no Dimension level.
- "Answered questionnaires" is undefined: feature.md:34 globs `docs/requirements/questionnaire-*.md`, which matches freshly generated, unanswered stubs equally; no criterion (e.g. non-empty `> Ответ:` lines) distinguishes them.
- /plan-improve's gate defines only Critical handling (plan-improve.md:63-66); the pipeline-wide Important-only rule routes findings into a Phase 3.5 digest that /plan-improve does not have — its Important-only outcome is undefined.
- ELICITATION.md's weight table says `light` → "minimal spec" but feature.md's mandatory spec shape applies at every weight; "minimal" is never defined.

## Lessons applied

None — `.claude/lessons/` does not exist in this repo (0 lesson files).

## Lessons NOT applied (and why)

None — no lesson files exist to evaluate.

## Memories to capture (suggested)

- `runstate-substitutes-v04`: v0.4 records playback approval as a greppable `**Approved by user:** <date> (weight: …)` line in the spec file (enforced by feature.md Phase 4's pre-condition) and ceremony weight in spec frontmatter — deliberate substitutes for the run-state ledger that does not exist until v0.7. — Stable design decision with rationale; v0.5–v0.7 work will otherwise implement against the spec's literal "run-state" wording.
