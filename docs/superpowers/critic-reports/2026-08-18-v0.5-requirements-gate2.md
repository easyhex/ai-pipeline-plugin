# Critic Review — Gate 2 — 2026-08-18

**Subject:** `git diff main..feature/v0.5-requirements` (25 files, +535/−58) against `docs/superpowers/specs/2026-08-18-v0.5-requirements-design.md`
**Reviewed against:** docs/architecture.md (n/a — plugin repo), CLAUDE.md (contributor hard rules), spec D1–D4, .claude/lessons/ (0 files)

**Spec fidelity summary:** all 9 shipped items are present; D1–D4 all implemented as decided. Partials: item 2 (amend flow — mint/amend conflict in `/improve`, see Critical), item 3 (ADR route undefined at the `/plan-improve` gate), item 4 (`/plan-improve` override does not append a risk row), item 9 (glossary/analogs carry `doc_version` no command ever bumps; version block is mid-file, not frontmatter). `scripts/check.sh`: 39/39 green, verified by running it.

## Critical (must address before proceeding)

- **`/improve` inherits Phase 3.5's unconditional mint and will create a duplicate F-ID + requirements file on every run.** `improve.md:10` says "same pipeline as /feature (all phases)" and `improve.md:48-58` lists deltas for Phases 5, 8, 9, 11 — but none for Phase 3.5. `feature.md:147-149` mints a new `F_ID` and creates `docs/requirements/<F_ID>-<slug>.md` on every playback approval. So an /improve run applies the update-vs-new rubric at ground (`improve.md:38`, "amend the file in place"), then Phase 3.5 mints a fresh file anyway — two contradictory instructions in one run; the feature ends up with two requirement files and the mid-diff history forks. `/improve` needs an explicit Phase 3.5 delta: amend path → no mint, touch the existing file; successor path → mint with a defined rule (see Important #2). — evidence: commands/improve.md:10,38,48-58; commands/feature.md:147-149

## Important (strongly suggest addressing)

- **`/improve` amends the requirements file only at Phase 11 — after gate-2 — so the new "requirements drift" check fires by construction on every improvement.** `agents/senior-critic.md:50` tells gate-2 to flag "behavior not present in the feature's docs/requirements/ file"; `improve.md:57` defers the FR/NFR amendment to Phase 11 (post-merge). During an /improve gate-2 the diff always implements behavior the requirements file doesn't yet contain. The "deliberate break" note (`improve.md:38`) covers only BREAKING rows. Either amend (or stage the amendment) before Phase 8, or tell the gate-2 critic which FR/NFR changes are pending. — evidence: commands/improve.md:38,57; agents/senior-critic.md:50
- **F-ID uniqueness is derived only from `features.md`, whose F-ID line appears at Phase 5 and is committed only at Phase 11 — abandoned or concurrent runs double-mint.** `feature.md:148` scans "ALL sections of docs/features.md"; the ID first lands there at `feature.md:186` (uncommitted until `feature.md:539`). A run abandoned between 3.5 and 5, a reverted working tree, or two parallel runs re-mints the same F-NNN over an existing `docs/requirements/F-NNN-*.md`. Additionally, a successor file opened by /improve gets an F-ID that never enters features.md (`improve.md:14,54` forbid adding lines) — permanently invisible to the scan. The mint rule matches the spec (item 7) verbatim, so this is a spec-level gap the implementation surfaces: scan `docs/requirements/` and `docs/TRACEABILITY.md` too. — evidence: commands/feature.md:148,186,539; commands/improve.md:14,54
- **The requirements file is untracked from Phase 3.5 until Phase 11; in the worktree path it is invisible where gate-2 runs.** Phase 6 (`feature.md:190-197`) creates the worktree after the mint; an untracked file in the primary checkout does not exist in the worktree, yet gate-2 (`feature.md:232`) is pointed at it. First `git add` of `docs/requirements/` is `feature.md:539` (Phase 11). An abandoned run leaves an untracked orphan, which feeds the double-mint above. Commit the spec + requirements file at Phase 3.5 approval (a natural durable record of the approval anyway). — evidence: commands/feature.md:147-149,190-197,230-234,538-540
- **`/plan-improve`'s gate override does not append a `docs/risks.md` row, contradicting two shipped template claims.** `plan-improve.md:65-70` defines `override` as "apply with written justification appended to the report" — no risk row. `assets/templates/risks.md:3` promises rows "appended AUTOMATICALLY whenever a critic-gate finding is override-d"; `assets/templates/CLAUDE.md:33` says "Every gate `override` appends a `docs/risks.md` row". Spec item 4 says the same. — evidence: commands/plan-improve.md:65-70; assets/templates/risks.md:3; assets/templates/CLAUDE.md:33
- **`[decision]`-marked suggestions at the `/plan-improve` gate have no ADR route.** `senior-critic.md:139-141` promises unconditionally "The orchestrator then also writes a committed ADR"; the ADR branch exists only in `feature.md:257` (inherited by /improve via Phase 8 reference and by /fix via `fix.md:85`). `plan-improve.md:57-63` restates the memory-write protocol without the ADR branch — a decision marked at a plan-improve gate becomes a memory only, silently. Reference the feature.md ADR branch there (per contributor rule 7: reference, don't duplicate) or state explicitly that plan-improve's gate never emits ADRs. — evidence: commands/plan-improve.md:57-63; agents/senior-critic.md:139-141; commands/feature.md:257
- **Hard rule 1 drift: `PIPELINE.md` — the declared source of truth — still describes the pre-v0.5 shape in three places, and template CLAUDE.md contradicts itself.** (1) `assets/templates/PIPELINE.md:11`: "/plan-improve … Loads the three plan files"; (2) the Phase 1 ground row lists only architecture/features/roadmap + lessons, while `feature.md:34-37` now also reads glossary, risks, analogs, and requirement files; (3) the `/init` section doesn't mention writing/seeding risks/glossary/analogs. Also `assets/templates/CLAUDE.md:59` ("Master Plan (architecture/features/roadmap)") contradicts line 37's four-file claim, and `plan-improve.md:2`'s description still says "(architecture / features / roadmap)". — evidence: assets/templates/PIPELINE.md:11 and Internal-phases table row 1; assets/templates/CLAUDE.md:37 vs 59; commands/plan-improve.md:2
- **`senior-critic.md` "You receive" lists were not updated to match the new "Look for" duties.** Gate-1 receive (`senior-critic.md:17-21`) omits risks.md/glossary/analogs required by bullets 28-31; gate-2 receive (`senior-critic.md:38-43`) omits the requirements file and risks.md required by bullets 50-51. `feature.md` passes them in the prompt, but the agent file is the standalone contract (plan-improve's invocation, for one, passes only "Master Plan files"). — evidence: agents/senior-critic.md:17-21,28-31,38-43,50-51
- **`/release` mid-diff is per-path, so renames and deletions silently corrupt the changelog.** `release.md:32-35` compares each *current* `docs/requirements/F-*.md` against the same path at `$LAST_TAG`; a renamed file (REQUIREMENTS_FORMAT.md explicitly says mids "survive renames") hits the `|| :` fallback and reports every pre-existing mid as "added", and files that existed at `$LAST_TAG` but no longer exist are never visited, so their mids are never classified "removed". Mid matching must be global: collect all mids across `$LAST_TAG:docs/requirements/` and compare against all current mids. — evidence: commands/release.md:32-38; assets/templates/REQUIREMENTS_FORMAT.md (mid rules)
- **`/release` first-release behavior is self-contradictory.** `release.md:12` says empty `LAST_TAG` → "skip diffing", but Phase 4's CHANGELOG template unconditionally contains "Requirements changed since <LAST_TAG>" (`release.md:53`) and Phase 3's comment ("new file → all requirements are added") implies the opposite policy. If Phase 3 runs anyway with `LAST_TAG` empty, `git show ":docs/…"` reads the index and silently diffs nothing. Define it: first release either lists all requirements as "added" or omits the section — one sentence. — evidence: commands/release.md:12,35,53

## Nice to have

- `feature.md:243` parses memory bullets "of the form `` - `<slug>`: … ``" — a `[decision]`-prefixed bullet (`- [decision] `<slug>`: …`, senior-critic.md:140) does not match that stated shape; an orchestrator following the parse instruction literally skips the memory half of a decision suggestion. Widen the stated form. — evidence: commands/feature.md:243,257; agents/senior-critic.md:140
- `plan-improve.md:17-21` Phase 1 loads only architecture/features/roadmap, yet the new change types F (retire risk rows) and G (analogs/glossary) require reading `docs/risks.md`/`docs/analysis/analogs.md`/`docs/glossary.md`. — evidence: commands/plan-improve.md:17-21,32-33
- Machine-filled analogs rows (WebSearch at `/init`, init.md:273) bypass the Phase 4.5 line-by-line confirm (init.md:289-292 covers features/roadmap/constraints only) and carry no `(proposed — unconfirmed)` tags — yet gate-1 treats "avoid" rows as findings-generating ground truth (senior-critic.md:29). — evidence: commands/init.md:273,289-292
- `glossary.md` and `analogs.md` templates carry `doc_version` + Change history, but `plan-improve.md:78` bumps only architecture/features/roadmap/risks — type-G edits never bump them. — evidence: commands/plan-improve.md:78; assets/templates/glossary.md; assets/templates/analogs.md
- The risks.md Open table has 7 columns (incl. `Source`), but the override instruction (`feature.md:123`) enumerates 6 values — `Source` (which gate) is never specified. — evidence: commands/feature.md:123; assets/templates/risks.md
- Spec item 9 says "doc_version + last_changed frontmatter"; the implementation places the `---` block mid-file (after the H1/Status line), so it is not frontmatter and renders as a stray paragraph between horizontal rules. Greppable, so functional. — evidence: assets/templates/architecture.md:7-9 and siblings
- `init.md:273` hardcodes the analogs question in Russian, drifting from the "prose in the conversation's language" rule (template CLAUDE.md rule 9) for non-Russian users. — evidence: commands/init.md:273
- `/fix` gate-2 inherits `feature.md`'s inputs verbatim (`fix.md:85` "Same as /feature Phase 8") including "Requirements file: docs/requirements/<F_ID>-<slug>.md" — /fix has no F_ID in scope; the critic is handed a nonexistent path. — evidence: commands/fix.md:85; commands/feature.md:232
- Test strength: **T27**'s EARS assertion is `grep -qc 'While\|When\|Where\|If'` — any one of those words anywhere passes (and `-qc` is redundant: `-q` short-circuits `-c`); losing 4 of the 5 patterns would still be green. **T29/T31** pass on any mention of `risks.md`/`TRACEABILITY` — the ground-read bullet alone satisfies T29 even if the override append were deleted. — evidence: scripts/check.sh:222-227,246-250,255-258
- Untested shipped behaviors: the C1 mint/amend conflict (no test asserts /improve does not re-mint), the override→risks append specifically (vs any mention), first-release `/release` handling, mid-diff rename robustness, glossary seeding in init.md (T32 greps only `analogs` there), and `[decision]` routing outside /feature. — evidence: scripts/check.sh:220-283

## Lessons applied

None matched the trigger of any lesson. (`.claude/lessons/` contains 0 files in this repo — noted, not a failure.)

## Lessons NOT applied (and why)

None — no lesson files exist in this repository.

## Memories to capture (suggested)

- `inheritance-delta-checklist`: `/improve` and `/fix` run `/feature`'s phases verbatim except for explicitly listed deltas — any new stateful step added to a `/feature` phase (mint, file write, commit) must get a matching delta or exclusion in `improve.md` and `fix.md` in the same change — this is stable structural knowledge about how the three commands compose, and its omission is exactly what produced this gate's Critical finding.

