# Critic Review — Gate 2 — 2026-08-18

**Subject:** `git diff main..feature/v0.6-math` (19 files, +292/−16) — v0.6 math layer
**Reviewed against:** docs/superpowers/specs/2026-08-18-v0.6-math-design.md (APPROVED, D1-D4), contributor CLAUDE.md hard rules 1-10, branch tree at feature/v0.6-math. No `.claude/lessons/` directory exists in this repo (0 files). `scripts/check.sh` re-run against an extracted branch tree: all green (49 pass sites).

## Critical (must address before proceeding)

- **Phase 9c is unreachable for exactly the projects it was built for.** Four skip paths inside Phase 9b still jump to Phase 10, bypassing 9c: `HAS_FRONTEND=no → skip directly to Phase 10` (commands/feature.md:309), `MODE=skip → skip to Phase 10` (:321), `SKIP_VISUAL=yes → skip to Phase 10` (:332, :373). A pure compute project has no frontend (line 309 fires), and /init now sets `visual_verify.mode: skip` for compute classes (init.md:230-234), so line 321 fires even if a frontend is somehow detected. Only the 9b Apply-verdict lines (:485-487) were retargeted to 9c. Following the command literally, a numerical-library/simulation/data-pipeline run never executes the quant gate. /improve and /fix inherit the same dead path, so their 9c deltas are dead too. T39-T46 cannot catch this: no test asserts the 9b skip targets. — evidence: commands/feature.md:309,321,332,373 vs :489

- **`docs/model.md` is write-only after /init — spec item 2 shipped only its template.** The spec (§ What ships, item 2) requires "Owned by /plan-improve; read in ground; versioned like Master Plan docs." None of the three integrations exist: (a) commands/plan-improve.md has no model.md request type (types A-G, :31-35), omits it from the versioned-doc bump list (:80), the size-budget check (:82-84), and the `git add` line (:92) — its stated owner cannot even commit it; (b) /feature Phase 1 ground (feature.md:27-38) does not read docs/model.md; (c) the gate-1 critic invocation (feature.md Phase 3 Inputs, :104-113) does not pass it — the only reference is one "Look for" clause in senior-critic.md:33 pointing at a file the critic was never given. The template's own note ("Owned by /plan-improve ... read in every ground phase", assets/templates/model.md:3) documents behavior that does not exist. T44 checks only file existence + an init.md mention, so this shipped untested. — evidence: git grep 'model.md' on branch: 3 hits, all init/template/one critic clause

## Important (strongly suggest addressing)

- **Undefined 9c mode when `QMODE=skip` but NFRs declare proving commands.** The gate runs in that case (feature.md:501), but the Apply rules (:511) only define outcomes for `required` and `best_effort`. A `partial`/`failed` verdict under `QMODE=skip` has no defined action. The spec (item 1: "skip for ui/service unless NFRs declare commands") implies a fallback mode — it is never stated. — evidence: commands/feature.md:501 vs :511

- **Phantom mutation threshold + trivial pass in `required` mutation mode.** 9c step 4 says `required` → "survivors above the declared threshold FAIL the gate," but no threshold field exists anywhere in `pipeline.quant_verify` (assets/templates/settings.json:25-35) or the CLAUDE.md table. And with `mutation: required` but `mutation_command` empty, nothing runs and nothing fails — the anti-overclaim rule (step 7) doesn't catch it because an unset command is not a "declared" check. Hard-rule-8-style drift: an instruction references a settings field the schema does not define. — evidence: commands/feature.md:506 vs assets/templates/settings.json

- **Vacuous `verified` with zero collected checks.** Step 7 grants `verified` when "every declared oracle/proving command was actually executed and passed" — vacuously true when the requirements file is missing, has no NFR proving commands, and all global commands are empty. A compute-class `required`-mode run then gets `verified` on an empty manifest. Related gap: REQUIREMENTS_FORMAT.md:48 claims "The quant-verify gate refuses `verified` for a requirement whose declared evidence was not exercised," but 9c step 2 only collects NFR proving commands + global commands — FR-level `verify.evidence` (test ids, derivation docs) is never gathered or executed, so that refusal has no mechanism. — evidence: commands/feature.md:503,509 vs assets/templates/REQUIREMENTS_FORMAT.md §Verification method

- **9c failure is a dead end; the critic's quant-evidence input is unreachable in the canonical flow.** `partial`/`failed` + `required` → "STOP ... do NOT merge" with no route back into tasks or the critic (compare gate-2's `address` → `bd create` → Phase 7 loop, feature.md:268-270). The Error handling summary table (:598-608) has no row for 9b or 9c failures. This matches the 9b precedent, but the consequence is new: senior-critic.md:59 reads quant evidence "if exists," yet Phase 8 always precedes 9c and nothing ever re-runs Phase 8 after 9c writes evidence — within the defined flow that input can never exist. — evidence: commands/feature.md:511, :598-608; agents/senior-critic.md:59

- **Claims-check quarantine is not executable as ordered.** Stage 1 demands tracing the diff "WITHOUT reading the spec" (senior-critic.md:69), but the gate-2 "You receive" list names the spec as input #2 (:52) and earlier checklist items ("Behaviors claimed in the spec but missing in the code," :63-area) require the spec before the quarantine bullet is reached. An agent reading the file top-to-bottom has always already loaded the spec by line 69. The stage-1 instruction must precede spec ingestion to mean anything. — evidence: agents/senior-critic.md:52 vs :69

- **Gate-1 critic cannot resolve "compute-class."** The new gate-1 duties key on project class ("Compute-class spec lacks `## Mathematical approach`... → Critical", senior-critic.md:35) and on docs/model.md (:33), but neither `.claude/settings.json` → `pipeline.project_class` nor docs/model.md appears in the critic's gate-1 "You receive" list (:17-23) or in feature.md's Phase 3 Inputs (:104-113). Weight is resolvable (spec frontmatter, :19); class is not — the critic is told to apply class-conditional severities without being told where the class lives. — evidence: agents/senior-critic.md:17-23,33-35; commands/feature.md:104-113

## Nice to have

- init.md's jq skips visual for `cli` too (init.md:232), but init.md's own Round-1 prose (:159) and the template CLAUDE.md claim (:126, "compute classes skip visual") mention only the three compute classes — the behavior is sensible, the documentation is incomplete (hard rule 1 territory).
- Test-strength gaps that let the two Criticals through: T40's canonicality check greps the literal `quant-evidence` (a paraphrased duplicate of 9c passes); T44 tests model.md existence, not ownership/ground-read; T45 checks `Mathematical approach` in senior-critic.md but not `Interface contracts`; no test asserts 9b's skip targets route through 9c.
- `seeds: []` behavior is unstated: "once per seed" with an empty array means zero executions (→ `partial` via anti-overclaim) but an agent may plausibly run once without `SEED` — say which.
- D4 narrowing: the spec says "an NFR with no proving command is an Important finding"; 9c step 2 flags only NFRs with `verify.method: Test` — an NFR lacking a `verify:` field entirely (legal outside compute classes) with no command goes unflagged.
- CLAUDE.md versioning list order: the v0.6.0 bullet was inserted between v0.3.0 and v0.5.0 (list now reads 0.2, 0.3, 0.6, 0.5, 0.4). Cosmetic. Also the handoff claim "51 assertions" — the branch check.sh has 49 `pass` sites.

## Lessons applied

None matched — no `.claude/lessons/` directory exists in this repo (noted, not failed).

## Lessons NOT applied (and why)

None — no lesson files exist to evaluate.

## Memories to capture (suggested)

- `phase-insertion-jump-audit`: when inserting a new phase after an existing gate, audit EVERY earlier "skip to Phase N" jump target, not just the gate's final verdict lines — 9b's four skip lines silently bypassed 9c for all compute projects while the check-suite greps stayed green. Stable extension of inheritance-delta-checklist to intra-file control flow.
- `vacuous-verdict-guard`: universally-quantified acceptance rules ("verified iff every declared check passed") are vacuously satisfied by an empty check set; every gate verdict needs an explicit zero-checks branch (e.g. zero collected checks in required mode → `partial`, never `verified`).

