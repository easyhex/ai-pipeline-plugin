# Critic Review — Gate 2 — 2026-08-18

**Subject:** `git diff main..fix/v0.3.1-patches` (v0.3.1 patch release: 7 bug fixes + scripts/check.sh + version bump)
**Reviewed against:** CLAUDE.md (contributor rules), assets/templates/CLAUDE.md, assets/templates/PIPELINE.md, .claude/lessons/ (0 files — directory does not exist in this repo)

## Critical (must address before proceeding)
None.

## Important (strongly suggest addressing)
- **The /fix → 9b SPEC_FILE handoff depends on shell state surviving across separate bash blocks in separate command files.** `commands/fix.md:88` sets `SPEC_FILE` in its own fenced block; `commands/feature.md:257` applies the default in the "Read settings" block; the awk extraction consumes it at `commands/feature.md:322-323`. Claude Code bash invocations reset shell state between calls. If the executing agent runs these as separate Bash calls, the /fix override is lost, the default expands to `docs/superpowers/specs/<SLUG>.md` — a file that does not exist for /fix runs (the real file is `<SLUG>-diagnosis.md`) — awk fails, and `URLS` silently falls back to `/`. That is bug 2's symptom partially resurrected in degraded form. Phase 9b already shares `MODE`/`SKIP_VISUAL` across blocks within one file, but this handoff adds a cross-command-file state dependency, which is strictly more fragile. — evidence: commands/fix.md:88, commands/feature.md:255-257, 320-323
- **T4 encodes the stale literal, not the invariant.** `grep -rqE '6[- ](command|slash)'` over three files stays green while the count "7" is restated in `.claude-plugin/plugin.json:3`, `.claude-plugin/marketplace.json:9` (the plugin-entry description in the same file whose top-level description was just de-counted), `commands/init.md:297`, `README.md:3,19,28,90`, `README_RU.md:3,19,28,84,90`, and `assets/templates/CLAUDE.md:3`. All are currently accurate, but the next command addition re-creates exactly the drift class fix 4 targeted, and check.sh will not catch it. The one-file de-count of marketplace.json is internally inconsistent: line 3 de-counted, line 9 not. — evidence: scripts/check.sh:44 (T4), file:line list above
- **T2 tests token presence, not use.** `grep -q 'SPEC_FILE' commands/feature.md && grep -q 'SPEC_FILE' commands/fix.md` passes even if the extraction line at feature.md:323 is reverted to the hardcoded spec path, as long as the comment at feature.md:255 survives. The test does not verify that the awk command reads `"$SPEC_FILE"` or that the hardcoded path is gone from the extraction. — evidence: scripts/check.sh:37-40 (T2), commands/feature.md:322-323
- **The AGENTS.md rewrite lives inside the beads-managed marker block and no longer matches its content hash.** `<!-- BEGIN BEADS INTEGRATION v:1 profile:minimal hash:ca08a54f -->` (AGENTS.md:39) marks the section as beads-owned; a beads AGENTS.md regeneration will clobber the hand-edit and restore the push mandate. T5 catches this only when check.sh is run manually, so the contradiction with CLAUDE.md rule 6 can silently return between runs. The T5 comment acknowledges the risk; the exposure window remains. — evidence: AGENTS.md:39, scripts/check.sh:55-59 (T5)

## Nice to have
- **T7 is an exact-line regression guard, not an invariant check.** `grep -q '^\[ -f index.html \] && HAS_FRONTEND=yes$'` only catches the verbatim old line; any rephrased unconditional detection (`[ -e index.html ]`, reordered operands) passes. — evidence: scripts/check.sh:67-69
- **Missing `.claude/settings.json` still silently disables the console-error check.** With no settings file, jq errors, `FAIL_CONSOLE` is empty, and `[ "$FAIL_CONSOLE" = "true" ]` at feature.md:390 treats it as off. Pre-existing failure mode, unchanged by this patch — noted because the new `if/else` filter touched this exact line. — evidence: commands/feature.md:262, 390
- **T3's `bd-[0-9]` guard scopes only `commands/*.md`.** assets/templates/ and docs/ are currently clean (verified by repo-wide grep), so risk is low today. — evidence: scripts/check.sh:43

## Fix-by-fix verification (spec claims vs. diff)
1. **jq `// true` no-op** — closed. New filter is type-correct for `false`/absent/`null`/`true`; T1 executes the real filter extracted from feature.md against both fixtures (genuine RED-capable test). Consumption at feature.md:390 matches the "true"/"false" string outputs.
2. **SPEC_FILE parameterization** — closed on paper; see Important finding 1 for the cross-shell fragility. /improve needs no override (its spec path matches the default per improve.md:26); /fix's override path matches its Phase 2 save path exactly.
3. **bd- prefix assumption** — closed. Verified locally: `bd create --silent` exists and "Output[s] only the issue ID (for scripting)". T3b's grep matches the new line.
4. **Count drift** — closed for the three in-scope locations; see Important finding 2 for the residual restatements and the weak T4 guard.
5. **AGENTS.md push mandate** — closed; the rewrite correctly defers to CLAUDE.md rule 6 and states the override explicitly. See Important finding 4 for clobber exposure.
6. **Phantom paths** — closed beyond T6's scope: repo-wide grep (README×2, assets/templates/, commands/, agents/, docs/) finds zero `.claude/commands/` or `.claude/agents/` references.
7. **index.html detection** — closed and coherent with adjacent behavior: the old path hard-failed compute repos anyway (no `package.json` → `jq .scripts.dev` fails → exit 1 in required mode); the new rule skips cleanly. Documented in assets/templates/CLAUDE.md:94 consistently with feature.md:70 and feature.md:242-245.

## Contributor hard-rule audit
- **Rule 1 (template consistency):** pass. assets/templates/CLAUDE.md:94 detection paragraph matches feature.md logic; PIPELINE.md is silent on detection (no contradiction); feature.md Phase 2 hint updated to match Phase 9b.
- **Rule 2 (lesson schema):** pass — LESSON_FORMAT.md and all lesson-field references untouched.
- **Rule 3 (version bump both files):** pass — 0.3.1 in both; T4b now guards agreement permanently.
- **Rule 5 (two READMEs together):** pass with a note — neither README changed; neither describes the detection rule or spec paths, so no drift was introduced. README_RU.md:3 still says "6 слоёв контекста" (six context layers), which remains accurate.
- **Rule 7 (memory logic):** pass — feature.md memory grounding (lines 44-46) and gate-2 auto-write (lines 191-205) untouched; improve.md change is reference-text only.
- **Rule 8 (visual block canonical in feature.md):** pass — T9 verifies `browser_navigate` appears in exactly one command file; fix.md adds only the SPEC_FILE override, not the MCP block; no new settings fields introduced.
- **Visual drift checks:** N/A — no `docs/superpowers/visual-evidence/2026-08-18-v0.3.1-patches/` exists (plugin repo, no frontend; gate correctly does not apply).
- **check.sh run result:** all 13 assertions green, exit 0, on fix/v0.3.1-patches.

## Lessons applied
None — `.claude/lessons/` does not exist in this repo (the lessons system ships to user projects; the plugin repo itself has no lesson files).

## Lessons NOT applied (and why)
None — no lesson files exist to evaluate.

## Memories to capture (suggested)
- `beads-silent-id-capture`: beads issue ID prefixes are project-derived; scripts must capture IDs via `bd create --silent` (prints only the ID), never by grepping for a `bd-` prefix — stable tool fact that will recur in every command file touching beads.
- `command-bash-block-state`: fenced bash blocks in command files may execute as separate Bash tool calls with no shared shell state; cross-block variables (SPEC_FILE, MODE, SKIP_VISUAL) are a convention the executing agent must honor by running dependent blocks in one shell — a stable design constraint of this plugin's command architecture.
