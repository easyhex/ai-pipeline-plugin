# Critic Review — Gate 2 — 2026-08-20

**Subject:** `git diff main..feature/v1.1.0-adopt` (commit e03383f — v1.1.0: /init adopt mode + inverted pre-flight fix)
**Reviewed against:** CLAUDE.md (contributor rules), assets/templates/PIPELINE.md, assets/templates/features.md, commands/{init,feature,plan-improve,improve,fix,release,validate}.md, scripts/check.sh, .claude/lessons/ (0 files — directory empty)

**Verification executed:** `bash scripts/check.sh` → real output: **71 ok, 0 FAIL, "all green"** (T64 "missing Master Plan reads as unfilled (executed)", T64b, T65 all pass).

## Critical (must address before proceeding)

- **The inverted pre-flight bug survives verbatim in `commands/improve.md:19`** — `grep -q "UNFILLED" docs/architecture.md 2>/dev/null && echo unfilled || echo filled`. This is the exact line v1.1.0 claims to fix: on a bare repo with no `docs/architecture.md`, grep fails → `echo filled` → `/improve`'s Master Plan gate passes. The diff fixed feature.md and plan-improve.md only; improve.md duplicates the line literally (it does NOT inherit by reference the way fix.md does). No test covers it: T64 extracts and executes feature.md's line only; T64b greps plan-improve.md only. The 71-green run is therefore a wrong-reason pass for the release's headline claim "the inverted Master Plan pre-flight is fixed" — and a contributor hard-rule-1 violation (command-file drift). Evidence: commands/improve.md:19 vs commands/feature.md:13 and commands/fix.md:12 ("same check as `/feature`", which inherits correctly).

## Important (strongly suggest addressing)

- **Phase 3's fenced copy block contradicts the never-overwrite delta and got no in-place edit.** The adopt deltas live only in Phase 0 prose (init.md:24-29); Phase 6 received an in-place edit (init.md:358) but Phase 3 did not. An agent executing the Phase 3 fenced bash verbatim runs `cp "$TEMPLATE_DIR/gitignore" ./.gitignore` (init.md:232) — replacing, not appending, an existing .gitignore — and `cp "$TEMPLATE_DIR/settings.json" ./.claude/settings.json` (init.md:233), clobbering a pre-existing Claude Code project's own settings (permissions/hooks). Additionally, if the settings.json collision IS skipped per the delta, the unconditional jq write (init.md:240-243) mutates the user's own settings file and the rest of the template's `pipeline.*` keys are never merged — no merge strategy is defined for settings.json (unlike .gitignore's explicit append rule). Evidence: init.md:25 vs init.md:213-243.
- **Adopted `shipped` entries break the /release and /validate contracts.** `/release` Phase 1 step 2 collects "entries in `docs/features.md` Shipped section without an `in v` stamp" (release.md:13) — the first post-adopt `/release` sweeps every pre-existing adopted feature into the CHANGELOG as newly released, and Phase 5 (release.md:66) appends ` in vX.Y.Z` to a `shipped YYYY-MM-DD` stamp the adopt delta never specifies, pulling "one sentence from the spec's Goal" from specs that don't exist. `/validate` candidates are `docs/requirements/F-*.md` with `status: shipped` (validate.md:12) — adopted features have no requirements files, so they are invisible even to the "shipped unfalsifiable" listing (validate.md:15), which works off requirements files, not features.md. The features.md template invariant "Every shipped feature has a living requirements file" (assets/templates/features.md:8) is broken on day one of adoption. The delta needs a distinct stamp for adopted entries (e.g. `shipped pre-adoption`) that /release excludes and /validate can surface. Evidence: init.md:27, release.md:13+66, validate.md:12+15.
- **Existing codebase WITHOUT `.git`: adopt still runs `git add -A`.** The Phase 6 delta's only-pipeline-files rule is conditioned on "the repo already exists" (init.md:28), and the in-place edit covers only the "git init fails because already a git repo" branch (init.md:356-358). Detection is source-file-based (init.md:18), so ADOPT=yes in an un-versioned codebase falls through to the main block `git init; git add -A` (init.md:346-353), sweeping the user's entire uncommitted tree into the scaffold commit — exactly what the delta forbids. The rule must key on `ADOPT=yes`, not on git-init failure. Evidence: init.md:28 vs init.md:346-353.
- **Shipped-claim drift in three places (hard rule 1).** (1) commands/init.md:2 — the frontmatter description shown in `/help` still says "Run once per project in an empty folder."; (2) commands/init.md:10 still says "designed to be safe in an empty folder and refuse in a folder that already has source code"; (3) assets/templates/PIPELINE.md:149 failure table still says "`/init` in non-empty folder | Refuse with explanation; suggest using an empty folder" — contradicting the updated PIPELINE.md:8 in the same shipped template. Evidence: file:line as cited.

## Nice to have

- Phase 0 ordering: the adopt ❓ (check 1) is asked before the CLAUDE.md refusal (check 2) — in a repo with both source code and a CLAUDE.md the user consents to adopt and is then refused. Run check 2 first. Evidence: init.md:16-38.
- Two commit messages for the same adopt path: the delta mandates `chore: adopt ai-pipeline (Master Plan derived from existing code)` (init.md:28) while the Phase 6 in-place fallback — the branch that actually fires in an adopted git repo — says `chore: pipeline scaffold via /init` (init.md:358).
- Phase 5's epic placeholder "Initial development of <app slug>" (init.md:334) misleads in adopt mode — the app is not in initial development.
- Test strength: T65 is keyword-presence only (grep for "adopt"/"never overwrite"/"proposed — unconfirmed") and cannot detect any of the Important findings above; T64b asserts absence of the old string rather than executing plan-improve's line. Consistent with house style, but the adopt contract is enforced by prose alone.

## Verified OK (explicit checks requested)

- `bash scripts/check.sh` → 71 ok / 0 fail, "all green" (real output).
- Corrected check line quoting: identical in feature.md and plan-improve.md; `[ -f ... ] && ! grep -q "UNFILLED" ... && echo filled || echo unfilled` short-circuits correctly for missing file / UNFILLED present / filled; the trailing parenthetical comment in feature.md sits outside the backticks, so T64's regex extracts the pure command.
- T64 is a genuine executed test (temp dir, both directions); the `\\"`→`"` unescape is a harmless no-op for the current line.
- fix.md inherits correctly by reference ("same check as `/feature`", fix.md:12).
- Phase 3's verify-all-copied loop does NOT falsely print `✗ MISSING`/STOP on skipped collisions — `test -f` (init.md:259) checks existence, and the colliding user file exists.
- The `-A` fix did not leak into the fresh-folder path: the main Phase 6 block (init.md:346-348) still uses `git add -A`, correct for an empty folder.
- No conflict between adopt and the CLAUDE.md refusal: the delta reaffirms check 2 stops (init.md:25), and PIPELINE.md's /init description matches (modulo the :149 table drift above).
- Semver: 1.1.0 is honest — adopt mode is additive new functionality (a declined offer still stops, so prior behavior is a strict subset), the pre-flight fix is a bug fix; both `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` bumped to 1.1.0; CLAUDE.md versioning-policy row added.

## Lessons applied

None matched — `.claude/lessons/` is empty (0 files). Noted, not failed.

## Lessons NOT applied (and why)

None (no lesson files exist in this repo).

## Memories to capture (suggested)

- `preflight-duplication-map`: the Master Plan pre-flight line is canonical in feature.md, inherited by reference in fix.md, but duplicated LITERALLY in plan-improve.md and improve.md — any fix to it must touch all three literal copies or convert improve.md/plan-improve.md to by-reference. Stable structural knowledge that just caused a shipped-bug near-miss.

```json
{"gate": 2, "critical": 1, "important": 4, "nice": 4, "status": "fail"}
```
