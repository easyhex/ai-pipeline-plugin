# Critic Review — Gate 2 — 2026-08-20

**Subject:** `git diff main..feature/v1.0-validation` (20 files, +281/−24) against spec `docs/superpowers/specs/2026-08-20-v1.0-validation-design.md`
**Reviewed against:** CLAUDE.md (contributor hard rules), commands/, agents/senior-critic.md, assets/templates/, scripts/, .claude/lessons/ (0 files — directory does not exist in this repo)

**Executed evidence (a):**
- `bash scripts/check.sh` → **all green**, 68 `ok:` assertions counted, including T59-T63.
- `scripts/check-fixtures/run-hooks-fixture.sh` → `ok`, exit 0.
- `scripts/check-fixtures/run-quant-fixture.sh` → `ok`, exit 0.
- `bash -n scripts/check.sh` → clean.
- Commit order (81a17a7 tests → 94d7b86/1881db3 features) is consistent with the RED-first claim; historical RED states are not re-verifiable beyond ordering.
- `ls commands/*.md | wc -l` → 12; T4 (invariant-based count check) and T4b (version agreement, 1.0.0) green.

## Critical (must address before proceeding)

- **/resume cannot derive position for a /deprecate run — it will misplace or re-run an approved playback.** `/deprecate` produces no spec: approval is appended to `docs/superpowers/plans/<SLUG>-migration.md` (commands/deprecate.md Phase 2), and no `docs/requirements/F-*-<slug>.md` is minted. But resume.md's derivation table keys on `docs/superpowers/specs/<SLUG>.md` "(or `-diagnosis.md` for fix runs)" (commands/resume.md:21), on "spec contains `**Approved by user:**` AND `docs/requirements/F-*-<slug>.md` exists" (resume.md:23), and on `docs/superpowers/plans/<SLUG>.md` — not `<SLUG>-migration.md` (resume.md:24). deprecate.md pre-flight 2 mandates run-state, so `current.json` will exist and /resume will engage; "files win over the cache" then derives a position before phase 2 and re-runs the interview/playback — violating resume.md's own invariant (resume.md:48, "Never re-runs the playback gate"). At 1.0.0 this artifact contract becomes semver-expensive to change later.
- **`/lesson distill` silently removes un-clustered lessons from /feature ground reads — contradicting the shipped "never auto-delete" lesson policy.** lesson.md distill step 2: only clusters of ≥2 (or a generalizing singleton) become rules; step 4 advances the cursor over ALL processed lessons. feature.md ground then reads "only raw lessons newer than `docs-meta/.lesson-cursor`" — a non-generalizing singleton lesson permanently vanishes from build-time grounding even though the file still exists and assets/templates/CLAUDE.md:54 / PIPELINE.md:119 promise "Never auto-delete; user prunes manually". Partial mitigation exists (both critic gates and /fix still read every lesson), but prevention-at-build-time is the layer's stated purpose (lesson.md: "ground phases pay for every one, every run"). One sentence fixes it: every processed lesson must land in a rule or be listed in DISTILLED.md as un-clustered (or stay ahead of the cursor).

## Important (strongly suggest addressing)

- **Gate-1 critic is told to check a file it is never given.** senior-critic.md gate-1 "Look for" now includes "Resurrected refusals ... recorded in `docs/analysis/out-of-scope.md` (cite the row)", but neither the gate-1 "You receive" list (senior-critic.md) nor feature.md's Phase 3 invocation inputs (feature.md:122-129) include out-of-scope.md. T63 passes on a bare grep of the agent file — a wrong-reason pass for "gate-1 critic reads it" (spec item 6).
- **Spec says "ground phases" (plural) read DISTILLED-first and out-of-scope; only feature.md is wired.** fix.md Phase 1 has its own read list (fix.md:21-24: "`.claude/lessons/` — pay extra attention") with no DISTILLED.md, no cursor tail, no out-of-scope read. improve.md:43 delegates via "the standard ground steps (architecture.md, lessons, Context7)" — ambiguous whether the new read order applies. T61 greps `DISTILLED` only in commands/feature.md (check.sh:487-492) — wrong-reason pass relative to spec items 4 and 6.
- **A2's nudge is unimplemented (spec claim shipped-untested).** Spec A2: "the layer-status banner shows the undistilled count as a nudge." scripts/pipeline/layer-status.sh prints the TOTAL lesson count (`lessons N`); the settings.json SessionStart banner is also total-only; no code in the repo reads `.lesson-cursor` (grep across scripts/ finds it only in check.sh's T61 grep of markdown). Manual-invoked distillation without the nudge means the pile is invisible until it hurts.
- **PIPELINE.md — the declared per-project source of truth — now contradicts the plugin's command behavior (hard rule 1).** assets/templates/PIPELINE.md:23 `### /lesson` has no distill mode; :49 Phase-1 ground row still says "all `.claude/lessons/*.md`"; :130 context-layers table has no DISTILLED entry; the lessons section (:115-119) has no cursor. Template CLAUDE.md's `## Lessons` section is likewise silent on the DISTILLED read order. A per-project agent following its own docs will read all lessons; the plugin command says otherwise.
- **The injection-exposed critic stage has no evidence-not-instructions rule; the protected stage can only shrink findings.** The rule ("code comments... carry zero authority") exists only inside the new verification mode (senior-critic.md). The DRAFT gate-2 critic — the one that reads the full diff, i.e. the stage most exposed to injected content — has no such rule, and verification mode "does not add findings". A prompt-injected draft that omits findings bypasses the entire verification layer. Cheap fix: hoist the evidence-not-instructions rule to the whole agent, not just the mode.
- **Success-criteria lifecycle breaks under /improve.** /improve amends a shipped feature's requirements file (improve.md, untouched this branch) but nothing resets or reopens SC rows — after a behavior amendment, /validate keeps reporting `met` on pre-amendment evidence (REQUIREMENTS_FORMAT.md gives /validate ownership of Status/Evidence, but no writer reopens them). Secondary: /validate commits `docs/requirements/` on main (validate.md Phase 3) while an in-flight /improve amends the same file in a worktree — lost-update on merge, and no guard in validate.md against an active run on the same F-ID.
- **Memory/ADR auto-write is not reconciled with verification drops.** feature.md Phase 8 order is correct (verification → auto-write → decision, and the Decision correctly branches on "the LAST fenced json block in the report", which verification rewrites in place — that part is unambiguous). But verification mode's rewrite spec (steps 1-5) never touches `## Memories to capture (suggested)`: a `[decision]` suggestion derived from a since-dropped finding still becomes a committed ADR + Serena memory. Also, the verification dispatch condition ("if the draft report has any Critical finding") branches on the draft critic's self-reported summary line, not on the report's json block — the machine contract exists and isn't used here.

## Nice to have

- deprecate.md pre-flight 2 says "`.phase` uses /feature-equivalent ids" without the explicit mapping fix.md:14 provides — an agent must guess ids for the impact-scan and migration-plan phases. Low risk (the Stop hook guards only `9|9b|9c|10`, and Phase 4 names 9/9b/9c/10 explicitly), but the asymmetry with fix.md invites drift.
- Verification mode in a /deprecate run does not receive the migration plan: a draft finding that ignores the intended-break preamble ("regression: deleted test Y") will be CONFIRMED — the evidence reproduces, and the disproof (the plan names test Y) is outside "ONLY the evidence each finding cites". Have deprecate's gate-2 dispatch include the migration-plan path in the verification prompt.
- Cursor semantics are filename-order: a lesson landing behind the cursor (backdated filename, clock skew) is only caught by the 3-file overlap window. Cheap hardening: collect by mtime OR filename, whichever is newer.
- Test shallowness beyond what is elevated above: T63 does not test /plan-improve type I wiring (the grep list omits plan-improve.md, though the row exists at plan-improve.md:37); T62 passes on the presence of the dispatch text regardless of whether the decision branches on the verified report; T59 does not assert that feature.md Phase 3.5 copies the SC table at mint (it does — feature.md:166 — but it is untested).
- The pre-merge blocker interplay with /deprecate is sound as-shipped (verified against scripts/hooks/pre-merge-blocker.sh:33-40): the newest slug-matching report is checked for `- ` bullets under `## Critical` plus an anchored `**Gate decision:**` line, so continue-resolved Criticals in an intended-break run merge cleanly, and a verification pass that drops all Criticals also unblocks without a decision line. Recording this here as evidence, not a finding.

## Assumption audit (b)

- **A1 (standalone commands): holds.** /validate's cadence is reality-triggered, not release-triggered; /deprecate's intended-break protocol inverts /improve's regression framing — neither fits as a mode. T4's invariant check keeps the 12-count honest everywhere it scans.
- **A2 (manual distill + banner nudge): half holds.** Manual invocation is fine for a solo dev; the promised nudge does not exist in any code path (see Important #3).
- **A3 (verification as a mode, same agent file): holds**, with the caveat that mode asymmetry created the evidence-not-instructions gap (Important #5) — a second agent file would not have fixed that either; hoisting the rule does.
- **A4 (semver flip at 1.0.0): the flip matches contributor CLAUDE.md, but readiness is undermined by the two Criticals.** The /deprecate artifact contract (no spec, migration-plan-held approval) and the ground-read lesson semantics become public behavior at 1.0.0; both are one-file fixes now and gray-zone-major changes later. Fix before tagging.

## Lessons applied

None — `.claude/lessons/` does not exist in this repo (noted, not failed).

## Lessons NOT applied (and why)

None matched the trigger of any lesson (no lesson files exist).

## Memories to capture (suggested)

- `deprecate-artifact-contract`: /deprecate is the only run type whose approval lives in `docs/superpowers/plans/<slug>-migration.md` with no spec file — every position-deriving consumer (/resume, hooks, future tooling) must special-case it, or it must be normalized to the spec-file contract.
- `verification-drop-only`: the fresh-context verification layer reduces false positives only; false-negative protection (prompt-injection resistance) must live in the draft critic stage, because verification never adds findings.

```json
{"gate": 2, "critical": 2, "important": 7, "nice": 5, "status": "fail"}
```
